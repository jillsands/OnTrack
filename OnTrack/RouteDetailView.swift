//
//  RouteDetailView.swift
//  OnTrack
//
//  Created by Jillian Sands on 7/31/22.
//

import SwiftUI

struct RouteDetailView: View {
    var route: Route
    
    var body: some View { /*
        let pathName = route.color + "Stops"
        List(pathName) { stop in
            HStack {
                Text(stop.title)
            }
            
        }
    }
}

struct RouteDetailView_Previews: PreviewProvider {
    static var previews: some View {
        RouteDetailView(route: routesData.first!) */
    }
}
